const initialState = {
  activeDate: null,
  activePrimary: null,
  activeSecondary: null,
  detailedFilter: null,
  secondaryFilteredData: null,
  filteredData: null,
}

export const filterReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
